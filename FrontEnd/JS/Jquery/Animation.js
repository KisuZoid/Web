//animation using jQuery 
//Note: animation using jQuery is not recommended. Use CSS animation instead, but basically used with event listeners.

// $("h1").fadeOut(); //--> will fade out the h1 tag. It will fade out the h1 tag. The h1 tag will be faded out.

// $("h1").fadeIn(); //--> will fade in the h1 tag. It will fade in the h1 tag. The h1 tag will be faded in.

// $("h1").fadeToggle(); //--> will fade toggle the h1 tag. If the h1 tag is faded out, it will fade it in. If the h1 tag is faded in, it will fade it out.

// $("h1").slideUp(); //--> will slide up the h1 tag. It will slide up the h1 tag. The h1 tag will be slid up.

// $("h1").slideDown(); //--> will slide down the h1 tag. It will slide down the h1 tag. The h1 tag will be slid down.

// $("h1").slideToggle(); //--> will slide toggle the h1 tag. If the h1 tag is slid up, it will slide it down. If the h1 tag is slid down, it will slide it up.


//cutom animation (self designed)
// $("h1").animate({opacity: 0.5}); //--> will animate the h1 tag. It will change the opacity of the h1 tag to 0.5. The h1 tag will be animated.

// $("h1").animate({opacity: 0.5, margin: "20px"}); //--> will animate the h1 tag. It will change the opacity of the h1 tag to 0.5 and the margin of the h1 tag to 20px. The h1 tag will be animated.

// $("h1").slideDown().slideUp().animate({opacity: 0.5}); //--> will slide down the h1 tag, then slide up the h1 tag, and then animate the h1 tag to opacity 0.5. The h1 tag will be slid down, then slid up, and then animated to opacity 0.5.